name: Trigger build in PokeHebrew repository
on:
  create:
    ref_type: tag
  issues:
    types: [ opened ]

jobs:  
  trigger-pokehebrew:
    runs-on: ubuntu-latest
    steps:
      - name: Create comment
        uses: peter-evans/create-or-update-comment@v1
        with:
          repository: Nog-Frog/PokeHebrew
          issue-number: 1
          token: ${{ secrets.PAT }}
          body: "Test comment for 'poking' the PokeHebrew repo."
